# RLHF 训练核心依赖 - CPU和集群通用
# 精简版，只包含必要包，固定版本号

# ============================================
# 核心框架
# ============================================
torch==2.1.0

# ============================================
# HuggingFace生态
# ============================================
transformers==4.36.0
datasets==2.14.0
accelerate==0.25.0
safetensors==0.4.0
huggingface-hub==0.20.0

# ============================================
# RLHF训练
# ============================================
trl==0.11.0

# ============================================
# 参数高效微调
# ============================================
peft==0.7.0

# ============================================
# 数据处理
# ============================================
numpy==1.24.3
pandas==2.0.3
pyarrow==14.0.1
dill==0.3.7

# ============================================
# 工具包
# ============================================
tqdm==4.66.0
PyYAML==6.0.1
requests==2.31.0
regex==2023.10.3

# ============================================
# 可选：分布式训练
# ============================================
# 如果需要分布式训练，取消注释
# deepspeed==0.12.3

# ============================================
# 注意事项
# ============================================
# 1. 不包含bitsandbytes，避免兼容性问题
# 2. 不包含triton，避免版本冲突
# 3. 所有版本都经过验证，确保兼容性
# 4. 如需GPU支持，确保CUDA版本匹配
# 5. 在CPU节点安装: pip install -r requirements_cpu_cluster.txt

# ============================================
# 安装命令
# ============================================
# CPU节点（联网）:
# pip install -r requirements_cpu_cluster.txt
#
# 集群（离线）:
# pip install --no-index --find-links=offline_assets/wheels/ -r requirements_cpu_cluster.txt
#
# 测试安装:
# python -c "from trl import PPOConfig, PPOTrainer; print('✅ TRL OK')"