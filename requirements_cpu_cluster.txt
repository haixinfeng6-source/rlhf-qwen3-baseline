# RLHF 训练核心依赖 - CPU和集群通用
# 精简版，只包含必要包，固定版本号

# ============================================
# 核心框架
# ============================================
# 根据CUDA版本选择（CUDA 12.8使用以下版本）
# 安装命令: pip install torch==2.7.0+cu128 --index-url https://download.pytorch.org/whl/cu128
torch==2.7.0+cu128

# ============================================
# HuggingFace生态
# ============================================
transformers==4.46.0
datasets==3.1.0
accelerate==1.0.0
safetensors==0.5.0
huggingface-hub==0.26.0

# ============================================
# RLHF训练
# ============================================
trl==0.11.4

# ============================================
# 参数高效微调
# ============================================
peft==0.13.2

# ============================================
# 数据处理
# ============================================
numpy==1.24.3
pandas==2.0.3
pyarrow==14.0.1
dill==0.3.7

# ============================================
# 工具包
# ============================================
tqdm==4.66.0
PyYAML==6.0.1
requests==2.31.0
regex==2023.10.3

# ============================================
# 可选：分布式训练
# ============================================
# 如果需要分布式训练，取消注释
# deepspeed==0.12.3

# ============================================
# 注意事项
# ============================================
# 1. 不包含bitsandbytes，避免兼容性问题
# 2. 不包含triton，避免版本冲突
# 3. PyTorch版本需根据CUDA版本选择
# 4. CUDA 12.8 使用 torch==2.7.0+cu128
# 5. CUDA 12.1 使用 torch==2.5.1+cu121
# 6. 在CPU节点安装: pip install -r requirements_cpu_cluster.txt

# ============================================
# 安装命令
# ============================================
# CUDA 12.8（推荐）:
# pip install torch==2.7.0+cu128 --index-url https://download.pytorch.org/whl/cu128
# pip install -r requirements_cpu_cluster.txt
#
# CUDA 12.1:
# pip install torch==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# pip install -r requirements_cpu_cluster.txt
#
# 集群（离线）:
# pip install --no-index --find-links=offline_assets/wheels/ -r requirements_cpu_cluster.txt
#
# 测试安装:
# python -c "import torch; print(f'PyTorch: {torch.__version__}, CUDA: {torch.cuda.is_available()}')"
# python -c "from trl import PPOConfig, PPOTrainer; print('✅ TRL OK')"