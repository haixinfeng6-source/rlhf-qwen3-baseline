# 无网络集群解决方案

## 🎯 问题描述

在无网络集群上运行 `python test_environment.py` 时出现以下失败：

```
Python Version.......................... ✓ PASS
CUDA & GPU.............................. ✓ PASS
Required Packages....................... ❌ FAIL
Disk Space.............................. ❌ FAIL
Network................................. ❌ FAIL
Training Flow........................... ❌ FAIL
```

## 🔍 失败原因分析

### 1. Required Packages ❌ FAIL
- **原因**: 依赖包未安装
- **解决方案**: 使用离线安装

### 2. Disk Space ❌ FAIL  
- **原因**: 原测试要求至少 50GB 空闲空间
- **解决方案**: 降低要求到 20GB（离线模式）

### 3. Network ❌ FAIL
- **原因**: 集群无网络连接
- **解决方案**: 使用离线资源，跳过网络检查

### 4. Training Flow ❌ FAIL
- **原因**: 测试从网络加载 GPT-2 模型
- **解决方案**: 改为从本地加载离线模型

## 🚀 快速解决方案

### 方案 1: 使用修复工具（推荐）

```bash
# 运行修复工具
python fix_offline_cluster.py

# 测试修复后的环境
python test_offline_cluster.py

# 开始训练
bash quick_start_offline.sh
```

### 方案 2: 手动修复

#### 步骤 1: 安装依赖包

```bash
# 检查是否有 wheel 文件
ls offline_assets/wheels/*.whl | wc -l

# 安装依赖
bash install_offline.sh

# 或手动安装
pip install --no-index --find-links=./offline_assets/wheels -r requirements.txt
```

#### 步骤 2: 验证安装

```bash
# 检查核心包
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "import transformers; print(f'Transformers: {transformers.__version__}')"
python -c "import trl; print(f'TRL: {trl.__version__}')"
```

#### 步骤 3: 使用专用测试脚本

```bash
# 使用无网络专用测试脚本
python test_offline_cluster.py
```

#### 步骤 4: 开始训练

```bash
# 单卡训练
bash run_offline.sh

# 多卡训练
bash run_offline_multi_gpu.sh
```

## 📋 专用工具说明

### 1. `test_offline_cluster.py`
专为无网络集群设计的测试脚本：
- 不检查网络连接
- 降低磁盘空间要求（20GB）
- 检查本地离线资源
- 使用本地模型测试训练流程

### 2. `fix_offline_cluster.py`
一键修复工具：
- 检查并安装依赖包
- 验证离线资源完整性
- 创建离线配置文件
- 生成快速启动脚本

### 3. `quick_start_offline.sh`
快速启动脚本：
- 自动检查环境
- 交互式确认
- 自动开始训练

## 🔧 环境要求检查

### 最低要求（离线模式）
- Python 3.8+
- CUDA 可用
- 20GB 磁盘空间
- 离线资源完整

### 离线资源检查
```bash
# 检查资源完整性
python -c "
import os
paths = [
    './offline_assets/models/Qwen--Qwen3-8B',
    './offline_assets/datasets/openbmb--UltraFeedback', 
    './offline_assets/wheels'
]
for p in paths:
    if os.path.exists(p):
        print(f'✓ {p}')
    else:
        print(f'❌ {p}')
"
```

### 资源大小
```bash
# 检查资源大小
du -sh offline_assets/
du -sh offline_assets/models/
du -sh offline_assets/datasets/
du -sh offline_assets/wheels/
```

## 📊 测试结果对比

### 原测试 (`test_environment.py`)
```
Python Version.......................... ✓ PASS
CUDA & GPU.............................. ✓ PASS
Required Packages....................... ❌ FAIL  # 需要安装
Disk Space.............................. ❌ FAIL  # 要求50GB
Network................................. ❌ FAIL  # 无网络
Training Flow........................... ❌ FAIL  # 需要网络
```

### 专用测试 (`test_offline_cluster.py`)
```
Python 版本............................. ✓ 通过
CUDA 和 GPU............................. ✓ 通过  
依赖包 (离线)........................... ✓ 通过  # 已安装
离线资源................................ ✓ 通过  # 本地资源
磁盘空间 (离线)......................... ✓ 通过  # 要求20GB
离线训练流程............................ ✓ 通过  # 本地测试
```

## 🛠️ 故障排除

### 问题 1: 依赖包安装失败

**错误信息**:
```
ERROR: Could not find a version that satisfies the requirement...
```

**解决方案**:
```bash
# 检查 wheel 文件
ls offline_assets/wheels/

# 手动安装核心包
pip install --no-index --find-links=./offline_assets/wheels torch
pip install --no-index --find-links=./offline_assets/wheels transformers
pip install --no-index --find-links=./offline_assets/wheels trl
```

### 问题 2: 模型加载失败

**错误信息**:
```
OSError: Model not found
```

**解决方案**:
```bash
# 检查模型路径
ls -lh offline_assets/models/Qwen--Qwen3-8B/

# 使用绝对路径
python train_rlhf.py --model_name=$(pwd)/offline_assets/models/Qwen--Qwen3-8B
```

### 问题 3: 显存不足

**错误信息**:
```
RuntimeError: CUDA out of memory
```

**解决方案**:
```bash
# 减小 batch size
python train_rlhf.py \
  --model_name=./offline_assets/models/Qwen--Qwen3-8B \
  --per_device_train_batch_size=1 \
  --gradient_accumulation_steps=16 \
  --use_8bit \
  --gradient_checkpointing
```

## 📝 完整工作流程

### 在有网络的机器上
```bash
# 1. 下载资源
python download_assets.py
pip download -r requirements.txt -d offline_assets/wheels

# 2. 传输到集群
rsync -avz offline_assets/ username@cluster:/path/to/project/
```

### 在无网络集群上
```bash
# 1. 进入项目目录
cd /path/to/project

# 2. 修复环境
python fix_offline_cluster.py

# 3. 测试环境
python test_offline_cluster.py

# 4. 开始训练
bash quick_start_offline.sh
```

## 💡 最佳实践

1. **先修复后测试**: 先运行 `fix_offline_cluster.py`，再运行 `test_offline_cluster.py`
2. **使用专用脚本**: 使用 `run_offline.sh` 而不是 `run_multi_gpu.sh`
3. **监控资源**: 训练前检查 GPU 显存和磁盘空间
4. **保存日志**: 训练日志保存在 `output_offline` 目录
5. **定期检查点**: 每 500 步保存检查点

## 📚 相关文档

- `离线运行指南.md` - 离线运行完整指南
- `资源清单.md` - 资源完整性检查
- `OFFLINE_SETUP.md` - 离线部署详细步骤
- `README.md` - 项目概述

## 🎯 成功标志

当看到以下输出时，表示无网络集群环境已就绪：

```
============================================================
测试总结
============================================================
Python 版本............................. ✓ 通过
CUDA 和 GPU............................. ✓ 通过
依赖包 (离线)........................... ✓ 通过
离线资源................................ ✓ 通过
磁盘空间 (离线)......................... ✓ 通过
离线训练流程............................ ✓ 通过

✓ 所有检查通过！可以开始离线训练。

启动训练:
  bash run_offline.sh              # 单卡
  bash run_offline_multi_gpu.sh    # 多卡
============================================================
```

---

**现在你可以在完全无网络的环境下运行 RLHF 训练了！** 🚀
