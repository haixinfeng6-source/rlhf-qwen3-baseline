# RLHF 训练依赖包
# Python 版本要求: 3.8+

# 核心框架
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.11.0  # 需要 0.11+ 支持新的导入结构

# 量化支持
bitsandbytes>=0.41.0

# 训练工具
tensorboard>=2.14.0
wandb>=0.15.0  # 可选，用于实验跟踪
rich>=14.0.0  # TRL 0.11+ 需要

# 其他依赖
numpy>=1.24.0
scipy>=1.10.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# 注意事项:
# 1. 如果安装失败，请先更新 pip: pip install --upgrade pip
# 2. 如果网络问题，使用国内镜像: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# 3. 如果 Python 版本过低，请升级到 3.8+
