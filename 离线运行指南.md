# 离线运行快速指南

## 🎯 前提条件

你已经在当前机器上下载了所有资源（15.39 GB）：
- ✓ 模型文件在 `./offline_assets/models/Qwen--Qwen3-8B`
- ✓ 数据集在 `./offline_assets/datasets/openbmb--UltraFeedback`
- ✓ 依赖包在 `./offline_assets/wheels` (84个wheel文件)

---

## 📋 场景 1: 在当前机器上离线运行

### 步骤 1: 安装依赖包

**Windows:**
```cmd
install_offline.bat
```

**Linux/Mac:**
```bash
bash install_offline.sh
```

**或手动安装:**
```bash
pip install --no-index --find-links=./offline_assets/wheels -r requirements.txt
```

### 步骤 2: 开始训练

**单卡训练:**
```bash
python train_rlhf.py ^
  --model_name=./offline_assets/models/Qwen--Qwen3-8B ^
  --dataset_name=./offline_assets/datasets/openbmb--UltraFeedback ^
  --output_dir=./output_offline ^
  --use_lora ^
  --use_8bit ^
  --gradient_checkpointing
```

**多卡训练 (4卡):**
```bash
# Windows
run_multi_gpu.bat

# Linux
bash run_multi_gpu.sh
```

---

## 📋 场景 2: 传输到无网络开发机运行

### 步骤 1: 打包资源

**方式 A: 直接传输（推荐）**
```bash
# 使用 rsync (支持断点续传)
rsync -avz --progress offline_assets/ username@server:/path/to/project/offline_assets/

# 或使用 scp
scp -r offline_assets username@server:/path/to/project/
```

**方式 B: 先压缩再传输**
```bash
# 压缩
tar -czf offline_assets.tar.gz offline_assets/

# 传输
scp offline_assets.tar.gz username@server:/path/to/project/

# 在开发机上解压
ssh username@server
cd /path/to/project
tar -xzf offline_assets.tar.gz
```

**方式 C: 使用移动硬盘**
- 将 `offline_assets` 目录复制到移动硬盘
- 物理传输到开发机
- 从移动硬盘复制到项目目录

### 步骤 2: 在开发机上安装依赖

```bash
# 进入项目目录
cd /path/to/project

# 安装依赖
bash install_offline.sh

# 验证安装
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "import transformers; print(f'Transformers: {transformers.__version__}')"
python -c "import trl; print(f'TRL: {trl.__version__}')"
```

### 步骤 3: 在开发机上开始训练

**单卡训练:**
```bash
python train_rlhf.py \
  --model_name=./offline_assets/models/Qwen--Qwen3-8B \
  --dataset_name=./offline_assets/datasets/openbmb--UltraFeedback \
  --output_dir=./output_offline \
  --use_lora \
  --use_8bit \
  --gradient_checkpointing
```

**多卡训练 (4卡):**
```bash
bash run_multi_gpu.sh
```

---

## 🔧 配置多卡训练脚本

如果需要修改 `run_multi_gpu.sh` 使用离线路径：

```bash
#!/bin/bash

# 多卡 RLHF 训练脚本（离线版本）

export CUDA_VISIBLE_DEVICES=0,1,2,3

torchrun \
    --nproc_per_node=4 \
    --master_port=29500 \
    train_rlhf.py \
    --model_name=./offline_assets/models/Qwen--Qwen3-8B \
    --dataset_name=./offline_assets/datasets/openbmb--UltraFeedback \
    --output_dir=./output_offline \
    --num_train_epochs=1 \
    --per_device_train_batch_size=1 \
    --gradient_accumulation_steps=8 \
    --learning_rate=1e-5 \
    --use_lora \
    --lora_r=16 \
    --lora_alpha=32 \
    --use_8bit \
    --gradient_checkpointing \
    --logging_steps=10 \
    --save_steps=500 \
    --max_length=512
```

---

## ✅ 验证资源完整性

运行验证脚本确认所有资源可用：

```bash
python test_offline_assets.py
```

**期望输出:**
```
============================================================
测试总结
============================================================
依赖包: ✓ 通过
模型: ✓ 通过
数据集: ✓ 通过

✓ 所有测试通过！可以开始离线训练。
```

---

## 📊 资源清单

| 资源 | 路径 | 大小 | 状态 |
|------|------|------|------|
| 模型 | `./offline_assets/models/Qwen--Qwen3-8B` | 14.2 GB | ✓ |
| 数据集 | `./offline_assets/datasets/openbmb--UltraFeedback` | 0.79 GB | ✓ |
| 依赖包 | `./offline_assets/wheels` | 0.41 GB | ✓ |
| **总计** | | **15.39 GB** | **✓** |

---

## 🚨 常见问题

### 1. 安装依赖时报错

**问题:** `ERROR: Could not find a version that satisfies the requirement...`

**解决:**
```bash
# 检查 wheels 目录是否存在
ls offline_assets/wheels/

# 确认有 84 个 wheel 文件
ls offline_assets/wheels/*.whl | wc -l

# 手动安装核心依赖
pip install --no-index --find-links=./offline_assets/wheels torch
pip install --no-index --find-links=./offline_assets/wheels transformers
pip install --no-index --find-links=./offline_assets/wheels trl
```

### 2. 模型加载失败

**问题:** `OSError: Model not found`

**解决:**
```bash
# 检查模型文件是否存在
ls offline_assets/models/Qwen--Qwen3-8B/

# 确认有 4 个 safetensors 文件
ls offline_assets/models/Qwen--Qwen3-8B/*.safetensors

# 使用绝对路径
python train_rlhf.py --model_name=$(pwd)/offline_assets/models/Qwen--Qwen3-8B
```

### 3. 数据集加载失败

**问题:** `FileNotFoundError: Dataset not found`

**解决:**
```bash
# 检查数据集文件
ls offline_assets/datasets/openbmb--UltraFeedback/train/

# 确认有 arrow 文件
ls offline_assets/datasets/openbmb--UltraFeedback/train/*.arrow
```

### 4. CUDA 不可用

**问题:** `RuntimeError: CUDA out of memory` 或 `CUDA not available`

**解决:**
```bash
# 检查 CUDA
python -c "import torch; print(torch.cuda.is_available())"
python -c "import torch; print(torch.cuda.device_count())"

# 如果显存不足，减小 batch size
python train_rlhf.py \
  --model_name=./offline_assets/models/Qwen--Qwen3-8B \
  --per_device_train_batch_size=1 \
  --gradient_accumulation_steps=16 \
  --use_8bit \
  --gradient_checkpointing
```

---

## 💡 优化建议

### 显存优化
```bash
# 使用 8bit 量化 + LoRA + gradient checkpointing
python train_rlhf.py \
  --model_name=./offline_assets/models/Qwen--Qwen3-8B \
  --use_8bit \
  --use_lora \
  --lora_r=8 \
  --gradient_checkpointing \
  --per_device_train_batch_size=1
```

### 速度优化
```bash
# 使用多卡 + 更大的 batch size
torchrun --nproc_per_node=4 train_rlhf.py \
  --model_name=./offline_assets/models/Qwen--Qwen3-8B \
  --per_device_train_batch_size=2 \
  --gradient_accumulation_steps=4
```

---

## 📝 完整命令速查

### 安装
```bash
# Windows
install_offline.bat

# Linux
bash install_offline.sh
```

### 验证
```bash
python test_offline_assets.py
```

### 训练
```bash
# 单卡
python train_rlhf.py --model_name=./offline_assets/models/Qwen--Qwen3-8B --dataset_name=./offline_assets/datasets/openbmb--UltraFeedback --output_dir=./output_offline --use_lora --use_8bit

# 多卡
bash run_multi_gpu.sh
```

---

**准备就绪！现在可以在完全离线的环境下运行 RLHF 训练了。** 🚀
